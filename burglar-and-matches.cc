#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int num_matchboxes[m];
    int num_matches[m];

    for (int i = 0; i < m; i++) {
        cin >> num_matchboxes[i] >> num_matches[i];
    }

    return 0;
}
