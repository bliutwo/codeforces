#include <bits/stdc++.h>
using namespace std;

int main() {
    // solution here
    int n, m;
    cin >> n >> m;
    int numNames[n];
    for (int i = 0; i < n; i++) {
        cin >> numNames[i];
    }
    return 0;
}
